/* PRIMERO HAREMOS NUESTRO PROGRAMITA PLSQL HOLA MUNDA
*/
--ESTE ES UN COMENTARIO DE UNA SOLA LINEA

--DECLARE VARIABLES PERO ES OPCIONAL
SET SERVEROUTPUT ON;
BEGIN --MARCA INCIO DE LA LOGICA DE PROG
DBMS_OUTPUT.PUT_LINE('MI PRIMER PROGRAMA DE PLSQL');

--NULL;

--EXCEPTION  ESTE BLOQUE ES OPCIONAL YA QUE NO TODO EL CODIGO LANZARA EXCEPCIONES

END;
-- / LA SLASH SE UNA PARA CORRER ESTE BLOQUE DESDE LINEA DE COMANDO Y MARCA EL FIN DEL BLOQUE SQL

/*DECLARE 
EDAD INTEGER;
EDAD_DIAS INTEGER;
BEGIN
EDAD:=82;
EDAD_DIAS:=EDAD*365;
DBMS_OUTPUT.PUT_LINE('TU EDAD EN DIAS ES: '||EDAD_DIAS);
END;
/


CREATE TABLE HOLA(ID INTEGER, EDAD INT, NOMBRE VARCHAR2(120));*/

BEGIN 

FOR I IN 1..10 LOOP
DBMS_OUTPUT.PUT_LINE(I||'PRIMER CICLO');

INSERT INTO HOLA VALUES (I,42,'CHAVO MEDIOCRE');
END LOOP;
END;

SELECT * FROM HOLA
